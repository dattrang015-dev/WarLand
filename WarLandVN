local player = game.Players.LocalPlayer
local RS = game:GetService("ReplicatedStorage")
local UIS = game:GetService("UserInputService")
local TargetParent = (game:GetService("RunService"):IsStudio() and player:WaitForChild("PlayerGui") or game:GetService("CoreGui"))

-- [ XÓA BẢN CŨ ]
if TargetParent:FindFirstChild("WarLand_Final") then TargetParent.WarLand_Final:Destroy() end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "WarLand_Final"
ScreenGui.Parent = TargetParent
ScreenGui.ResetOnSpawn = false

-- [ GIAO DIỆN CHÍNH ]
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 580, 0, 380)
MainFrame.Position = UDim2.new(0.5, -290, 0.5, -190)
MainFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 15)
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)
local Stroke = Instance.new("UIStroke", MainFrame); Stroke.Thickness = 2.5; Stroke.Color = Color3.fromRGB(255, 255, 255)

-- TIÊU ĐỀ
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 45)
Title.Text = "WARLAND VN HUB (V5.5 FINAL)"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.BackgroundTransparency = 1; Title.Font = Enum.Font.GothamBold; Title.TextSize = 20
Title.Parent = MainFrame

-- THANH TAB (HÀNG NGANG)
local TabBar = Instance.new("Frame")
TabBar.Size = UDim2.new(1, -20, 0, 40)
TabBar.Position = UDim2.new(0, 10, 0, 45)
TabBar.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
TabBar.Parent = MainFrame
Instance.new("UICorner", TabBar).CornerRadius = UDim.new(0, 8)

local TabList = Instance.new("UIListLayout")
TabList.Parent = TabBar; TabList.FillDirection = Enum.FillDirection.Horizontal; TabList.Padding = UDim.new(0, 5)
TabList.HorizontalAlignment = Enum.HorizontalAlignment.Center; TabList.VerticalAlignment = Enum.VerticalAlignment.Center

-- KHU VỰC HIỂN THỊ NỘI DUNG
local Container = Instance.new("Frame")
Container.Size = UDim2.new(1, -20, 1, -100)
Container.Position = UDim2.new(0, 10, 0, 90)
Container.BackgroundTransparency = 1
Container.Parent = MainFrame

local Pages = {}

-- [ HÀM TẠO TRANG RIÊNG BIỆT ]
local function CreatePage(name)
    local Page = Instance.new("ScrollingFrame")
    Page.Size = UDim2.new(1, 0, 1, 0)
    Page.BackgroundTransparency = 1; Page.Visible = false; Page.ScrollBarThickness = 2
    Page.CanvasSize = UDim2.new(0,0,0,0); Page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    Page.Parent = Container
    Instance.new("UIListLayout", Page).Padding = UDim.new(0, 10)
    Pages[name] = Page
    
    local TabBtn = Instance.new("TextButton")
    TabBtn.Size = UDim2.new(0, 95, 0, 30)
    TabBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
    TabBtn.Text = name; TabBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
    TabBtn.Font = Enum.Font.GothamBold; TabBtn.TextSize = 14
    TabBtn.Parent = TabBar
    Instance.new("UICorner", TabBtn).CornerRadius = UDim.new(0, 6)
    
    TabBtn.MouseButton1Click:Connect(function()
        for _, p in pairs(Pages) do p.Visible = false end
        Page.Visible = true
        for _, b in pairs(TabBar:GetChildren()) do 
            if b:IsA("TextButton") then b.BackgroundColor3 = Color3.fromRGB(35, 35, 45) end 
        end
        TabBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 120)
    end)
    return Page
end

-- [ HÀM THÊM CHỨC NĂNG ]
local function AddToggle(parent, name, sub, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -10, 0, 50)
    btn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    btn.Text = "  " .. name .. "\n  " .. sub
    btn.TextColor3 = Color3.fromRGB(255, 255, 255); btn.TextXAlignment = Enum.TextXAlignment.Left
    btn.Font = Enum.Font.Gotham; btn.TextSize = 13; btn.Parent = parent
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    btn.MouseButton1Click:Connect(callback)
end

-- ================= KHỞI TẠO CÁC TAB RIÊNG BIỆT =================

local Home = CreatePage("home")
local Farm = CreatePage("Farm")
local Trial = CreatePage("TRIAL")
local Fruit = CreatePage("Fruit")
local PvP = CreatePage("pvp")

-- 🏠 TAB HOME: Thông tin và bảo mật
AddToggle(Home, "🛡️ ADMIN DETECTOR", "Tự thoát khi thấy Admin", function() end)
AddToggle(Home, "⚡ SUPER FPS BOOST", "Xóa texture giúp máy cực mượt", function() end)
AddToggle(Home, "📡 SERVER HOP", "Đổi server ngay lập tức", function() end)

-- ⚔️ TAB FARM: Cày cuốc siêu tốc
AddToggle(Farm, "🔥 AUTO FARM LEVEL", "Nhận Quest + Gom quái + Đánh", function() end)
AddToggle(Farm, "👹 AUTO FARM BOSS", "Tự tìm và diệt Boss trên Map", function() end)
AddToggle(Farm, "🍭 ULTRA BUG CANDY", "Đổi kẹo sự kiện siêu tốc (50 luồng)", function() end)

-- ⛩️ TAB TRIAL: Lên Tộc V4
AddToggle(Trial, "📍 TP TEMPLE OF TIME", "Bay đến Đền Thờ Thời Gian", function() end)
AddToggle(Trial, "⚙️ AUTO PULL LEVER", "Gạt cần (Cần Mirror Fractal)", function() end)
AddToggle(Trial, "💎 AUTO TRIAL V4", "Hỗ trợ vượt ải Trial", function() end)

-- 🍎 TAB FRUIT: Trái ác quỷ
AddToggle(Fruit, "🎲 RANDOM FRUIT", "Quay trái ác quỷ từ xa", function() RS.Remotes.CommF_:InvokeServer("Cousin","BuyItem") end)
AddToggle(Fruit, "📦 AUTO STORE FRUIT", "Cất trái đang cầm vào kho", function() end)
AddToggle(Fruit, "📤 DROP FRUIT", "Thả trái đang cầm", function() end)

-- 🔫 TAB PVP: Hỗ trợ chiến đấu
AddToggle(PvP, "🎯 AIM BOT", "Tự động khóa mục tiêu gần nhất", function() end)
AddToggle(PvP, "👻 NO CLIP", "Đi xuyên tường mọi nơi", function() end)
AddToggle(PvP, "🚀 SPEED HACK", "Chạy nhanh như Flash", function() end)

-- Mặc định hiện Tab Home
Pages["home"].Visible = true

-- [ KÉO THẢ & NÚT MỞ ]
local dragging, dragStart, startPos
MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = true; dragStart = input.Position; startPos = MainFrame.Position end
end)
UIS.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
UIS.InputEnded:Connect(function() dragging = false end)

local OpenBtn = Instance.new("TextButton")
OpenBtn.Parent = ScreenGui; OpenBtn.Size = UDim2.new(0, 50, 0, 50); OpenBtn.Position = UDim2.new(0, 10, 0.5, -25)
OpenBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25); OpenBtn.Text = "W"; OpenBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
Instance.new("UICorner", OpenBtn).CornerRadius = UDim.new(1, 0)
OpenBtn.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)
