--[[ 
    WARLAND VN HUB (V5.5 FINAL)
    - Tối ưu hóa đa luồng cho Ultra Bug
    - Hỗ trợ full lộ trình Up Tộc V2 -> V4
    - Hệ thống chống văng (Anti-Crash) tích hợp
]]

local player = game.Players.LocalPlayer
local RS = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local TS = game:GetService("TeleportService")
local UIS = game:GetService("UserInputService")
local TargetParent = (RunService:IsStudio() and player:WaitForChild("PlayerGui") or game:GetService("CoreGui"))

-- Xóa bản cũ
if TargetParent:FindFirstChild("WarLand_Ultimate") then TargetParent.WarLand_Ultimate:Destroy() end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "WarLand_Ultimate"
ScreenGui.Parent = TargetParent
ScreenGui.ResetOnSpawn = false

-- ================= GIAO DIỆN CHÍNH =================
local MainFrame = Instance.new("Frame")
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 15)
MainFrame.Position = UDim2.new(0.5, -280, 0.5, -210)
MainFrame.Size = UDim2.new(0, 560, 0, 420)
MainFrame.BorderSizePixel = 0
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)

-- VIỀN RGB CHUYỂN MÀU
local UIStroke = Instance.new("UIStroke")
UIStroke.Thickness = 2.8
UIStroke.Parent = MainFrame
task.spawn(function()
    while task.wait() do UIStroke.Color = Color3.fromHSV(tick() % 5 / 5, 0.7, 1) end
end)

-- THANH TIÊU ĐỀ & LOGO
local Header = Instance.new("Frame")
Header.Size = UDim2.new(1, 0, 0, 70); Header.BackgroundTransparency = 1; Header.Parent = MainFrame

local Logo = Instance.new("ImageLabel")
Logo.Parent = Header; Logo.Position = UDim2.new(0, 15, 0, 10); Logo.Size = UDim2.new(0, 50, 0, 50)
Logo.Image = "rbxassetid://9861125091"; Logo.BackgroundTransparency = 1; Instance.new("UICorner", Logo).CornerRadius = UDim.new(1, 0)

local Title = Instance.new("TextLabel")
Title.Parent = Header; Title.Text = "WARLAND VN | QUANTUM ULTIMATE"; Title.Position = UDim2.new(0, 75, 0, 20)
Title.TextColor3 = Color3.fromRGB(255, 255, 255); Title.Font = Enum.Font.GothamBold; Title.TextSize = 22; Title.BackgroundTransparency = 1; Title.TextXAlignment = Enum.TextXAlignment.Left

-- KHU VỰC CUỘN (SCROLLING)
local Content = Instance.new("ScrollingFrame")
Content.Parent = MainFrame; Content.Position = UDim2.new(0, 15, 0, 80); Content.Size = UDim2.new(1, -30, 1, -95)
Content.BackgroundTransparency = 1; Content.CanvasSize = UDim2.new(0, 0, 0, 0); Content.ScrollBarThickness = 2
Content.AutomaticCanvasSize = Enum.AutomaticSize.Y

local UIList = Instance.new("UIListLayout")
UIList.Parent = Content; UIList.Padding = UDim.new(0, 10)

-- HÀM TẠO NÚT (ADD BUTTON)
local function AddButton(name, sub, color, callback)
    local btn = Instance.new("TextButton")
    btn.Parent = Content; btn.Size = UDim2.new(1, -10, 0, 55); btn.BackgroundColor3 = Color3.fromRGB(22, 22, 28); btn.Text = ""
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    
    local t1 = Instance.new("TextLabel")
    t1.Text = "  " .. name; t1.Size = UDim2.new(1, 0, 0.6, 0); t1.TextColor3 = Color3.fromRGB(255, 255, 255); t1.Font = Enum.Font.GothamBold; t1.TextXAlignment = Enum.TextXAlignment.Left; t1.BackgroundTransparency = 1; t1.Parent = btn
    
    local t2 = Instance.new("TextLabel")
    t2.Text = "  " .. sub; t2.Size = UDim2.new(1, 0, 0.4, 0); t2.Position = UDim2.new(0, 0, 0.5, 0); t2.TextColor3 = color; t2.Font = Enum.Font.Gotham; t2.TextSize = 11; t2.TextXAlignment = Enum.TextXAlignment.Left; t2.BackgroundTransparency = 1; t2.Parent = btn
    
    btn.MouseButton1Click:Connect(function()
        local success, err = pcall(callback)
        if not success then warn("Lỗi tính năng: " .. err) end
    end)
end

-- ================= THIẾT LẬP CHỨC NĂNG =================

-- [ NHÓM CANDY & EVENT ]
AddButton("🍭 ULTRA BUG CANDY (50 THREADS)", "Tốc độ bàn thờ - Đổi kẹo liên tục", Color3.fromRGB(255, 0, 150), function()
    _G.UltraBug = not _G.UltraBug
    if _G.UltraBug then
        for i = 1, 50 do
            task.spawn(function()
                while _G.UltraBug do
                    pcall(function() RS.Remotes.CommF_:InvokeServer("CandyShop","Exchange") end)
                    task.wait()
                end
            end)
        end
    end
end)

-- [ NHÓM UP TỘC V2, V3, V4 ]
AddButton("⛩️ TP TEMPLE OF TIME", "Dịch chuyển đến Đền Thờ Thời Gian (V4)", Color3.fromRGB(200, 100, 255), function()
    player.Character.HumanoidRootPart.CFrame = CFrame.new(28282, 14891, 105)
end)

AddButton("💎 AUTO TRIAL V4", "Hỗ trợ đánh quái vượt ải Trial", Color3.fromRGB(255, 50, 50), function()
    _G.AutoTrial = not _G.AutoTrial
    task.spawn(function()
        while _G.AutoTrial do
            task.wait()
            for _,v in pairs(game.Workspace.Enemies:GetChildren()) do
                if v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 then
                    player.Character.HumanoidRootPart.CFrame = v.HumanoidRootPart.CFrame * CFrame.new(0,0,5)
                    RS.Remotes.CommF_:InvokeServer("Attack")
                end
            end
        end
    end)
end)

-- [ NHÓM SĂN TRÁI ]
AddButton("🍎 FRUIT STORM", "Hút sạch trái ác quỷ trên map về chân", Color3.fromRGB(0, 255, 255), function()
    for _,v in pairs(game.Workspace:GetChildren()) do
        if v:IsA("Tool") and v:FindFirstChild("Handle") then
            v.Handle.CFrame = player.Character.HumanoidRootPart.CFrame
        end
    end
end)

-- [ NHÓM ADMIN & BẢO MẬT ]
AddButton("🛡️ ADMIN DETECTOR", "Tự thoát khi thấy Admin (Bảo vệ nick)", Color3.fromRGB(255, 0, 0), function()
    game.Players.PlayerAdded:Connect(function(p)
        if p:GetRankInGroup(2830838) >= 100 then TS:Teleport(game.PlaceId, player) end
    end)
end)

AddButton("⚡ GOD FPS BOOST", "Xóa toàn bộ hiệu ứng, cực mượt cho mobile", Color3.fromRGB(0, 255, 0), function()
    for _,v in pairs(game:GetDescendants()) do
        if v:IsA("BasePart") then v.Material = Enum.Material.SmoothPlastic end
        if v:IsA("Decal") or v:IsA("Texture") then v:Destroy() end
    end
end)

-- ================= HỆ THỐNG ĐIỀU KHIỂN =================

-- KÉO THẢ (DRAG)
local dragging, dragStart, startPos
Header.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true; dragStart = input.Position; startPos = MainFrame.Position
    end
end)
UIS.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
Header.InputEnded:Connect(function() dragging = false end)

-- NÚT ĐÓNG/MỞ (FLOATING)
local OpenBtn = Instance.new("TextButton")
OpenBtn.Parent = ScreenGui; OpenBtn.Size = UDim2.new(0, 55, 0, 55); OpenBtn.Position = UDim2.new(0, 15, 0.5, -27)
OpenBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 30); OpenBtn.Text = "W"; OpenBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
OpenBtn.Font = Enum.Font.GothamBold; OpenBtn.TextSize = 20
Instance.new("UICorner", OpenBtn).CornerRadius = UDim.new(1, 0)
local BStroke = Instance.new("UIStroke", OpenBtn); BStroke.Thickness = 2; BStroke.Color = Color3.fromRGB(255, 255, 255)

OpenBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)
